#include "main.h"
/**
 * rev_string -> reverese
 * @s: parameter
 */
void rev_string(char *s)
{
int len;
int len2;
int len = 0;
int i;
for (; s[len] != '\0'; len++)
{
len2 = len;
}
for (i = 0; i <= len; len--)
{
_putchar(s[len]);
}
putchar('\n');
}
