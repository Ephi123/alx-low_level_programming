/**
 *add - perform adition
 *
 * Return: a + b
 */
int add(a, b)
  return a + b;

/**
 *sub - perform subtraction
 *
 * Return: a - b
 */
int sub(a, b)
  return a - b;

/**
 *mul - perform multiplication
 *
 * Return: a - b
 */
int mul(a, b)
  return a * b;

/**
 *div - perform div
 *
 * Return: a / b
 */
int div(a, b)
  return a / b;

/**
 *mod - perform mod
 *
 * Return: a - b
 */
int mod(a, b)
  return a % b;
  

  
